package main.intvs.int2023_24.altimetrics;

public class Round_01 {

    // how to create obj without @bean obj
    //  stale state in react,
    // compatible feature

    public static void main(String[] args) {
        System.out.println("Hello world!");
        // "abc", "xyz", "mlp"
        // "xyz", "mlp", "abc"

        // list.stream().sorted((a,b)-> b.compareTo(a));

        //list.stream().filter((emp)-> emp.age > 40).collect()

        // && (emp.gender.equals("M") || emp.gender.equals("F")))
        // .collect(Collectors.toList());

        // const [count, setCount] = useState<number>(0);

        // setCount((prevState)=> prevState+1);
        // console.log(count);

    }
}
